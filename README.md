# La Energia del Dinero

Web estática (HTML/CSS/JS) + APIs Node (Serverless Functions) en `api/`.

## Scripts
- `vercel dev` ? corre local (requiere Vercel CLI)
- `vercel --prod` ? despliega a producción

## Endpoints
- `/api/hello`
- `/api/time?tz=America/Lima`
